\section{Discussion}

% ===============================================================
% OUTLINE -- no figures in this section.
%   P1  what worked: the biomarker terms are cheap to add, are
%       descended identically at training and test time, and cut the
%       MTV/TLG bias by more than a factor of two over the baselines
%       without costing accuracy.
%   P2  the conceptual finding: the trade-off practitioners read as
%       "accuracy versus biomarker preservation" is, at the gradient
%       level, a regulariser-versus-data-term tension. Rigidity, not
%       the biomarker terms, carries almost all of the opposition.
%       This is the most transferable result in the paper -- give it
%       real space, and say what it implies for how such objectives
%       should be weighted or scheduled.
%   P3  what did not work: TLG is orthogonal to every other term while
%       holding 45% of its group's gradient norm -- loud, unopposed
%       and still underperforming. We read this as a proxy problem:
%       the term is a poor stand-in for what the metric measures.
%       Open question for future work.
%   P4  why derivative-based rigidity fails here and when it does not:
%       the failure is specific to structures thinner than the
%       derivative stencil; on thick bone the classical formulation is
%       presumably fine. Position the Kabsch penalty as the fix for
%       thin-structure regimes rather than a universal replacement.
%   P5  limitations: surrogate labels carry their own error; a single
%       challenge cohort; bone masks only a few voxels wide; validation
%       cases only, with no test-set numbers at the time of writing;
%       model selection performed on the same cases it is evaluated on.
%   P6  future work.
% ===============================================================

\todo{write the discussion following the outline above}

\section{Conclusion}
% OPTIONAL -- roughly half the 2025 proceedings papers end at the
% Discussion and half add a short Conclusion. Cut this first if the
% paper runs over the page limit.

\todo{optional short conclusion}
