\section{Methods}

% ===============================================================
% OUTLINE
%   2.1 Data
%   2.2 Preprocessing
%   2.3 Affine Pre-registration
%   2.4 Registration Pipeline          >>> FIGURE 1 (pipeline overview)
%   2.5 Loss Functions                 >>> FIGURE 2 (det J masking)
%   2.6 Instance Optimisation
%   2.7 Model Selection                >>> FIGURE 3 (selection trade-off)
%   2.8 Baselines
%   2.9 Implementation Details
% Quantitative diagnostics (gradient conflict, rigidity stencil, HD95
% floor) live in Sec.~\ref{sec:ablation}; only their motivation is here.
% ===============================================================

\subsection{Data and Preprocessing}
\label{sec:data_and_preprocessing}
% TODO: PSMAReg cohort description
%   - number of training / validation / test pairs, baseline+follow-up
%   - modalities (CT, PET), voxel spacing, field of view, scanners
%   - which labels the organisers release and which are withheld
%   - our surrogate labels are only NAMED here; they are defined in
%     Sec.~\ref{sec:evaluation}, since they are an evaluation choice
% A dataset table is possible but the 2025 proceedings papers do this
% in prose -- recommend prose.
%   - intensity normalisation: CT HU windowing, PET SUV normalisation
%   - label generation: CT organ labels (TotalSegmentator), PET lesion
%     labels (autoPET-III), bone masks

\subsection{Registration Pipeline}
\label{sec:pipeline}
%   - refer to figure 1
%   - 
%   - what is optimised, on which modality, with which similarity
%   - why a separate affine stage (large FOV and positioning shifts)
%   - note that the affine is in general NOT volume preserving, which
%     is why all biomarker terms are evaluated on the composed transform
%   - LapIRN recap: Laplacian pyramid, three coarse-to-fine sub-networks,
%     diffeomorphic parameterisation via a stationary velocity field
%   - what we changed: input channels (PET + CT), channel widths,
%     resolution per level, training schedule across levels
%   - DEFINE THE NOTATION HERE, not in the loss section:
%     the total transformation $\varphi = \varphi_{\mathrm{aff}} \circ
%     \varphi_{\theta}$, the moving PET image $I$, the moving lesion
%     mask $M$, and the warped lesion region $W$.



\subsection{Loss Functions}
\label{sec:losses}
% TODO: one short preamble paragraph giving the TOTAL objective as a
% single equation,
%   \mathcal{L} = \lambda_{NCC} L_NCC + \lambda_{DSC} L_DSC
%               + \lambda_{MTV} L_MTV + ... ,
% with the weights either inline or in a small table.
% PLACEHOLDER: the final weights are not fixed yet.

\subsubsection{Image similarity.}
%   - normalised cross correlation, window size, on which modality
%   - only on CT

\subsubsection{Weak label supervision (DSC).}
We used a standard DSC loss for weak label supervision.
Since the challenge scores PET organ overlap but provides no PET organ annotations for training, we measured each of the 117 TotalSegmentator organs' mean SUV relative to skeletal muscle across all sessions.
As 90 organs fall below twice the muscle background and are thus indistinguishable in PET, we upweighted only the 10 exceeding 4 times the background (kidneys, gallbladder, spleen, liver, duodenum, bladder, portal vein, prostate, pancreas) by a factor of 2 in the per-class Dice loss.
This relies on PET and CT being well aligned within a session, so that CT-derived overlap on these organs approximates the PET organ overlap being scored.


\subsubsection{Preservation of PET quantification.}
The scored MTV bias only constrains the \emph{net} lesion volume, so we penalize it directly while supplying dense gradients through a Jacobian-based surrogate.
All terms are evaluated on the total transformation $\varphi = \varphi_{\mathrm{aff}} \circ \varphi_{\theta}$, i.e.\ the affine pre-registration composed with the predicted displacement, against the original moving lesion mask $M$, since the affine itself is generally not volume-preserving.
First, we minimize the squared relative volume error
%
\begin{equation}
  \mathcal{L}_{\mathrm{MTV}}
  = \left( \frac{\lvert M \circ \varphi \rvert - \lvert M \rvert}
                {\lvert M \rvert} \right)^{2},
  \label{eq:mtv}
\end{equation}
%
where $M \circ \varphi$ denotes the linearly resampled mask.
This matches the scored metric exactly, but its gradient is confined to the lesion's sub-voxel boundary shell.
We therefore add a complementary term based on the change-of-variables identity: summing $\det J_{\varphi}$ over the warped lesion region recovers the original lesion volume, so the mean determinant over the warped lesion equals the ratio of original to warped volume. Driving this mean to one,
%
\begin{equation}
  \mathcal{L}_{\mathrm{MTV\text{-}J}}
  = \left( \frac{1}{\lvert W \rvert} \sum_{x \in W}
    \det J_{\varphi}(x) - 1 \right)^{2},
  \label{eq:mtvjac}
\end{equation}
%
where $W$ denotes the warped lesion region, therefore pins the same net volume ratio as~(\ref{eq:mtv}) while providing gradients throughout the lesion interior; unlike a per-voxel constraint, it still permits internal redistribution of volume.
Note that masking the determinant with the moving-frame lesion, as is tempting, samples $\det J_{\varphi}$ at locations unrelated to the lesion once the affine is composed in; empirically, this decorrelated the surrogate from the scored MTV (Fig.~\ref{fig:detj-masking}). Finally, the per-voxel penalty
%
\begin{equation}
  \mathcal{L}_{\mathrm{jac}}
  = \frac{1}{\lvert W \rvert} \sum_{x \in W}
    \left( \det J_{\varphi}(x) - 1 \right)^{2}
  \label{eq:jactum}
\end{equation}
%
acts as a stricter local regulariser, discouraging compensating compression and expansion within the lesion.

\begin{figure}[t]
\centering
\figplaceholder{0.22}{FIGURE 2 -- Schematic, two panels. (a) masking
$\det J_{\varphi}$ with the moving-frame lesion mask: once the affine is
composed in, the sampled locations are unrelated to the lesion.
(b) masking with the warped lesion region $W$: the sampled locations
coincide with the lesion. This is the subtle point of Eq.~(2) and a
diagram makes it instant. High value per column inch.}
\caption{Moving-frame versus warped-frame masking of the Jacobian
determinant. \todo{caption}}
\label{fig:detj-masking}
\end{figure}

The challenge additionally scores the total lesion glycolysis (TLG), the PET-activity--weighted counterpart of the MTV, which we penalize analogously as the relative change of intensity mass under the warp:
%
\begin{equation}
  \mathcal{L}_{\mathrm{TLG}}
  = \frac{\bigl\lvert
      \sum_{x} (I \circ \varphi)(x)\,(M \circ \varphi)(x)
      - \sum_{y} I(y)\, M(y)
    \bigr\rvert}
    {\sum_{y} I(y)\, M(y)},
  \label{eq:tlg}
\end{equation}
%
where $I$ denotes the moving PET image.
Since both the image and the mask are resampled with the same transformation, this term is sensitive not only to volume change but also to intensity blurring at the lesion boundary, and thus complements the purely geometric terms (\ref{eq:mtv})--(\ref{eq:jactum}).

The same terms (\ref{eq:mtv})--(\ref{eq:tlg}) are used during training and descended at test time by our instance optimisation (Sec.~\ref{sec:io}), so the network is trained on the objective it is later refined under.

\subsubsection{Bone rigidity.}
In 146 patients with paired baseline and follow-up scans, a median of 46.0\% (IQR 3.1 to 72.3\%) of total lesion volume at baseline lay within skeletal structures, rising to 63.8\% (IQR 12.1 to 77.4\%) at follow-up.
Since a substantial fraction of lesion volume is therefore intraosseous, a rigidity penalty on a bone mask is an anatomically grounded surrogate for the biomarker terms: enforcing rigid bone motion is both physiologically correct and, where lesions sit in bone, directly prevents the volume change MTV measures.

The established formulation is the local rigidity penalty of Staring et al.\ [staring2007] \todo{cite all rigidity papers and name the three conditions we used}, whose three conditions are evaluated by finite differences at every voxel and only then masked, so that the mask selects which voxels are scored but not which are read.
The derivative stencil therefore also reads non-bone voxels, and with thin cortical bone and ribs this dominates: the term ends up measuring the bone-to-soft-tissue displacement discontinuity that any correct registration must exhibit.
We quantify this failure mode in Sec.~\ref{sec:ablation}.
Eroding the mask would remove the effect, but in the present dataset many bone masks are only a few voxels wide.

We therefore replaced this loss with a per-structure rigid-fit residual: for each label, we obtain the optimal rotation and translation from the predicted displacement in closed form via Kabsch alignment, and penalise the mean squared residual of that fit.
No stencil crosses the mask, and each label is fitted independently, so articulation is free.
The penalty constrains only the shape of the deformation within each structure and is invariant to its placement, leaving alignment to NCC and Dice, unlike derivative based penalties [staring2007, modersitzki2008] and unlike F\"orner and Wendler [foerner2025], who regress the field onto a per bone rigid field precomputed by Dice pre registration, requiring segmentations in both images and inheriting pre registration error as a training target.
% TODO: write $\mathcal{L}_{\mathrm{rigid}}$ out as an equation --
% Kabsch fit per label, mean squared residual, averaged over labels.

\subsubsection{Deformation regularisation.}
To regularise the displacement field, we used a diffusion regulariser and a Jacobian loss that penalises non-diffeomorphic voxels.
% TODO: write both out as equations and give their weights.

\subsubsection{Losses considered and rejected.}
We deliberately do not add a differentiable surface-distance term (e.g.\ the
distance-transform Hausdorff surrogate of Karimi and Salcudean \todo{citation})
to either the training objective or the instance optimisation: a per-label
analysis of the validation predictions shows that most labels already lie at
the discretisation floor, while the mean HD95 is driven by a tail that a
boundary loss cannot address.
The supporting analysis is given in Sec.~\ref{sec:ablation}.

\subsection{Instance Optimisation}
\label{sec:io}
% TODO -- currently only a single clause in the loss section.
%   - which terms are descended at test time and with which weights
%     (the training/test objective parity is a selling point)
%   - optimiser, learning rate, number of iterations, stopping rule
%   - runtime cost per case, and its effect on each metric
%   - Code/instance_opt.py, Code/finetune_on_io.py

\subsection{Model Selection}
\label{sec:model_selection}

Model selection enters our pipeline twice: once \emph{within} a run, to decide which
checkpoint to keep, and once \emph{across} runs, to decide which variant to submit in
the container.
Both use the same three-component quality notion, and differ only in how each component
is scored.

Both stages combine registration accuracy (CT DSC and HD95), PET-biomarker preservation
(absolute MTV and TLG bias) and deformation regularity (\%NDV after the official
$0.005\%$ practical-equivalence adjustment) as a weighted geometric mean with exponents
$0.4$, $0.4$ and $0.2$, mirroring the challenge's final score
\begin{equation}
  S = 100 \cdot A^{0.4} \, B^{0.4} \, R^{0.2},
  \label{eq:score}
\end{equation}
where the accuracy term $A$ is the geometric mean of the DSC and HD95 scores, the
biomarker term $B$ that of the MTV and TLG scores, and $R$ the \%NDV score.

\subsubsection{Checkpoint selection.}
Registration accuracy and tumour preservation do not peak together: in level-3 training,
validation Dice improves until it plateaus, whereas the tumour metrics reach their
optimum roughly halfway through and degrade thereafter (Fig.~\ref{fig:selection}).
Selecting on Dice alone therefore yields a checkpoint well past the tumour optimum, so we
select on Eq.~\eqref{eq:score} instead.
The official per-metric scores are ranks within a cohort of competing methods, which does
not exist during a single training run.
We therefore replace each rank score with a logistic quality centred on the metric's
median and scaled by its robust spread (interquartile range divided by $1.349$), both
measured over the validation rounds of a completed run.

\begin{figure}[t]
\centering
\figplaceholder{0.26}{FIGURE 3 -- Two panels sharing an x-axis of
training steps: (a) validation Dice rising to a plateau, (b) MTV and TLG
bias reaching their optimum roughly halfway through and degrading
thereafter, with the checkpoint selected by Eq.~(5) marked in both.
This figure IS the argument of this subsection -- strongly recommended.}
\caption{Accuracy and biomarker preservation do not peak at the same
checkpoint. \todo{caption}}
\label{fig:selection}
\end{figure}

\subsubsection{Submission selection.}
Choosing between trained variants does provide a cohort, so here we evaluate
Eq.~\eqref{eq:score} exactly as the challenge does: each of the five metrics is ranked
separately across the $K$ variants, with ties taking the worst rank they span, and rank
$r$ is turned into the score $(K - r + 1)/K$.
The variant with the highest $S$ is the one we submit.
To check that this choice is not an artefact of a few cases, we additionally compare the
selected variant against every other variant per metric with a paired Wilcoxon
signed-rank test over the validation cases, Holm-corrected within each metric.
As the variant is selected on the same cases, these tests are exploratory rather than
confirmatory; the official validation leaderboard serves as the held-out check.

\subsection{Baselines}
\label{sec:baselines}
% TODO -- note: this subsection previously reused the label
% sec:model_selection, which silently broke every \ref to it.
%   - unregistered and affine-only references
%   - NiftyReg, ConvexAdam: versions, parameter settings, runtime
%   - vanilla LapIRN without the proposed loss terms
%   - state that all baselines are evaluated under the identical
%     protocol of Sec.~\ref{sec:evaluation}

\subsection{Implementation Details}
\label{sec:implementation}
% TODO -- every 2025 proceedings paper has this subsection.
%   - framework, GPU model and memory, mixed precision
%   - patch vs. full-resolution training, batch size
%   - optimiser, learning rate schedule, epochs/iterations per pyramid level
%   - training time in total, inference time per pair with and without IO
%   - submission container and reproducibility notes
% PLACEHOLDER: fill in once the final configuration is frozen.
